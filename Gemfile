source 'https://rubygems.org'

gem 'rake'
gem 'dotenv'

source 'https://cli.gemfury.com/ruby/' do
  gem 'gemfury'
end

group :development do
  gem 'rspec'
  gem 'parallel_tests'

  source 'https://repo.nlevel.io/ruby/' do
    gem 'docker_task', '~> 0.1.12'
  end

  #gem 'docker_task', :path => File.expand_path('~/work/docker_task')
end
